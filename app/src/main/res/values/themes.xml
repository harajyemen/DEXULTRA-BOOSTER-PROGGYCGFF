<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ── Base Theme ── -->
    <style name="Theme.DexUltraBoosterPro" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/colorOnError</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:windowBackground">@color/primary</item>
    </style>

    <!-- ── Splash Theme ── -->
    <style name="Theme.DexUltraBoosterPro.Splash" parent="Theme.DexUltraBoosterPro">
        <item name="android:windowBackground">@color/primary</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- ── Card Style ── -->
    <style name="DexCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/card_bg</item>
        <item name="cardCornerRadius">14dp</item>
        <item name="cardElevation">4dp</item>
        <item name="strokeColor">@color/card_border</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <!-- ── Button: Primary (Accent) ── -->
    <style name="DexButton.Primary" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/accent</item>
        <item name="android:textColor">@color/primary_dark</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">10dp</item>
        <item name="rippleColor">#330099BB</item>
    </style>

    <!-- ── Button: Secondary (Outlined) ── -->
    <style name="DexButton.Secondary" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/accent</item>
        <item name="android:textColor">@color/accent</item>
        <item name="cornerRadius">10dp</item>
        <item name="rippleColor">#220099BB</item>
    </style>

    <!-- ── Button: Boost (Large, Gradient-like) ── -->
    <style name="DexButton.Boost" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/accent</item>
        <item name="android:textColor">@color/primary_dark</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">28dp</item>
        <item name="rippleColor">#440099BB</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- ── Spinner Item ── -->
    <style name="DexSpinnerItem" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:background">@color/surface_elevated</item>
        <item name="android:padding">8dp</item>
    </style>

</resources>
